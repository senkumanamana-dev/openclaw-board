# Version: 2026-02-05-1200
services:
  - type: web
    name: openclaw-board
    env: node
    plan: free
    buildCommand: npm run render-build
    startCommand: npm run render-start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 22.22.0
      - key: DATABASE_URL
        fromDatabase:
          name: openclaw-board-db
          property: connectionString

databases:
  - name: openclaw-board-db
    databaseName: openclaw_board
    user: openclaw
    plan: free
